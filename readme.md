# LangChain Chatbot 🤖

A **simple, session-aware chatbot** built using the LangChain framework. This project demonstrates how to create a conversational AI that maintains session history — enabling context continuity between user messages.

---

## 🧠 Features

- 🗣️ **Interactive conversational interface**  
  Chat with a language model using text input.

- 📜 **Session history support**  
  Preserves chat history during a session to deliver contextually relevant responses.

- 🐍 **Built with Python & LangChain**  
  Uses LangChain for LLM orchestration and prompt management. :contentReference[oaicite:0]{index=0}

---

## 📦 Requirements

Before running this project, make sure you have:

- Python **3.8+**
- An active **OpenAI API key** (or any supported LLM provider)
- `pip` installed

---

## 🚀 Installation & Setup

1. **Clone the repository**

   ```bash
   git clone https://github.com/Sahilp14/langchain_chatbot.git
   cd langchain_chatbot
